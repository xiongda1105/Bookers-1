<h2>Editing Book</h2>
<%= form_for(@book, url: "/books/#{@book.id}") do |f| %>
  <p>Title</p>
  <%= f.text_field :title %>
  <p>Body</p>
  <%= f.text_area :body %>
  <%= f.submit "Update Book" %>
<% end %>
<%= link_to "Show", "/books/#{@book.id}" %>
<%= link_to "Back", "/books" %>